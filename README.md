# UPM_Proyecto_Secuencial
Development of a C++ cellular automaton simulating wildfire spread, inspired by Conway's Game of Life. This project benchmarks both a baseline sequential implementation and an optimized version using vectorization and optimization libraries, analyzing execution time, resource usage, and ease of deployment.
